---
import Box from "../Box/Box.astro";
import { stack } from "./stack.css";
import type { StackProps } from "./Stack.types";

const { class: classList, direction, ...props }: StackProps = Astro.props;
---

<Box class:list={[stack({ direction }), classList]} {...props}>
  <slot />
</Box>
