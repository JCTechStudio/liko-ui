---
import { Box } from "@liko-ui/astro";

const { class: classList } = Astro.props;
---

<Box
  class:list={[classList]}
  class="bg-[repeating-linear-gradient(135deg,var(--palette-solid)_0_2px,--alpha(var(--palette-solid)/50%)_1px_8px)]"
/>
