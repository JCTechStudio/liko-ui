---
title: Button
description: Permite a los usuarios realizar una acción o evento.
sidebar:
  badge:
    text: New!
    variant: success
---

import { TabItem, Tabs } from "@astrojs/starlight/components";
import { Button, HStack, Stack } from "@liko-ui/astro";
import CodePreview from "../../../../components/CodePreview.astro";
import { paletteColors } from "../../../../utils/colors.utils.ts";

## Uso básico

<Tabs>
  <TabItem label="Preview">
    <CodePreview>
      <Button>Button</Button>
    </CodePreview>
  </TabItem>
  <TabItem label="Code">
    ```astro showLineNumbers
    ---
    import {Button} from '@kori/ui';
    ---

    <Button>
      Button
    </Button>
    ```
  </TabItem>
</Tabs>

## Variantes

### Shape

<Tabs>
  <TabItem label="Preview">
    <CodePreview>
      <Button shape="sharp">Button</Button>
      <Button>Button</Button>
      <Button shape="circle">Button</Button>
    </CodePreview>
  </TabItem>
  <TabItem label="Code">
    ```astro ins=/shape="(?:sharp|circle)"/
    ---
    import {Button} from '@kori/ui';
    ---

    <Button shape="sharp">
      Button
    </Button>

    <Button>
      Button
    </Button>

    <Button shape="circle">
      Button
    </Button>
    ```
  </TabItem>
</Tabs>

### Fill

<Tabs>
  <TabItem label="Preview">
    <CodePreview>
      <Stack class="gap-4">
        {paletteColors.map(([name]) => (
          <HStack key={name} class="gap-4">
            <span className="min-w-[8ch]">{name}</span>
            <Button palette={name}>Button</Button>
            <Button palette={name} fill="tonal">Button</Button>
            <Button palette={name} fill="surface">Button</Button>
            <Button palette={name} fill="outline">Button</Button>
            <Button palette={name} fill="clear">Button</Button>
            <Button palette={name} fill="text">Button</Button>
          </HStack>
        ))}
      </Stack>
    </CodePreview>
  </TabItem>
  <TabItem label="Code">
    ```astro ins=/fill="(?:tonal|surface|outline|clear|text)"/
    ---
    import {Button, HStack, Stack} from '@kori/ui';
    ---

    <Stack class="gap-4">
      {paletteColors.map(([name]) => (
        <HStack key={name} class="gap-4">
          <span className="min-w-[8ch]">{name}</span>
          <Button palette={name}>Button</Button>
          <Button palette={name} fill="tonal">Button</Button>
          <Button palette={name} fill="surface">Button</Button>
          <Button palette={name} fill="outline">Button</Button>
          <Button palette={name} fill="clear">Button</Button>
          <Button palette={name} fill="text">Button</Button>
        </HStack>
      ))}
    </Stack>
    ```
  </TabItem>
</Tabs>

### Size

<Tabs>
  <TabItem label="Preview">
    <CodePreview>
      <HStack class="gap-4">
        <Button size="xs">Button (xs)</Button>
        <Button size="sm">Button (sm)</Button>
        <Button>Button (md)</Button>
        <Button size="lg">Button (lg)</Button>
        <Button size="xl">Button (xl)</Button>
      </HStack>
    </CodePreview>
  </TabItem>
  <TabItem label="Code">
    ```astro ins=/size="(?:xs|sm|lg|xl)"/
    ---
    import {Button, HStack} from '@kori/ui';
    ---

    <HStack class="gap-4">
      <Button size="xs">
        Button (xs)
      </Button>

      <Button size="sm">
        Button (sm)
      </Button>

      <Button>
        Button (md)
      </Button>

      <Button size="lg">
        Button (lg)
      </Button>

      <Button size="xl">
        Button (xl)
      </Button>
    </HStack>
    ```
  </TabItem>
</Tabs>

### Block

<Tabs>
  <TabItem label="Preview">
    <CodePreview>
      <Button block>Button</Button>
    </CodePreview>
  </TabItem>
  <TabItem label="Code">
    ```astro ins="block"
    ---
    import {Button} from '@kori/ui';
    ---

    <Button block>
      Button
    </Button>
    ```
  </TabItem>
</Tabs>

## Estados

### Disabled

<Tabs>
  <TabItem label="Preview">
    <CodePreview>
      <Button disabled>Button</Button>
    </CodePreview>
  </TabItem>
  <TabItem label="Code">
    ```astro ins="disabled"
    ---
    import {Button} from '@kori/ui';
    ---

    <Button disabled>
      Button
    </Button>
    ```
  </TabItem>
</Tabs>

### Loading

<Tabs>
  <TabItem label="Preview">
    <CodePreview>
      <HStack class="gap-4">
        <Button loading>
          Loading button with hidden long text
        </Button>
        <Button loading loadingText="Loading...">
          Loading button with hidden long text
        </Button>
        <Button loading loadingText="Loading..." spinnerPlacement="end">
          Loading button with hidden long text
        </Button>
      </HStack>
    </CodePreview>
  </TabItem>
  <TabItem label="Code">
    ```astro ins=/(?:loadingText="Loading..."|loadingSpinner="{}"|spinnerPlacement="end"|loading)/
    ---
    import {Button, HStack} from '@kori/ui';
    ---

    <HStack class="gap-4">
      <Button loading>
        Loading button with hidden long text
      </Button>

      <Button loading loadingText="Loading...">
        Loading button with hidden long text
      </Button>

      <Button loading loadingText="Loading..." spinnerPlacement="end">
        Loading button with hidden long text
      </Button>
    </HStack>
    ```
  </TabItem>
</Tabs>

### Spinner personalizado

<Tabs>
  <TabItem label="Preview">
    <CodePreview>
      <HStack class="gap-4">
        <Button
          loading
          loadingSpinner={
            <span className='
                size-4 border border-dashed border-black
                border-t-transparent rounded-full animate-spin' />
          }
        >
          Loading button with hidden long text
        </Button>
        <Button
          loading
          loadingText="Loading..."
          loadingSpinner={
            <span className='
                size-4 border border-dashed border-black
                border-t-transparent rounded-full animate-spin' />
          }
        >
          Loading button with hidden long text
        </Button>
        <Button
          loading
          loadingText="Loading..."
          loadingSpinner={
            <span className='
                size-4 border border-dashed border-black
                border-t-transparent rounded-full animate-spin' />
          }
          spinnerPlacement="end"
        >
          Loading button with hidden long text
        </Button>
      </HStack>
    </CodePreview>
  </TabItem>
  <TabItem label="Code">
    ```astro ins=/(?:loadingText="Loading..."|loadingSpinner="{}"|spinnerPlacement="end"|loading)/
    ---
    import {Button, HStack} from '@kori/ui';
    ---

    <HStack class="gap-4">
      <Button
        loading
        loadingSpinner={
          <span className='
                size-4 border border-dashed border-black
                border-t-transparent rounded-full animate-spin' />
        }
      >
        Loading button with hidden long text
      </Button>
      <Button
        loading
        loadingText="Loading..."
        loadingSpinner={
          <span className='
                size-4 border border-dashed border-black
                border-t-transparent rounded-full animate-spin' />
        }
      >
        Loading button with hidden long text
      </Button>
      <Button
        loading
        loadingText="Loading..."
        loadingSpinner={
          <span className='
                size-4 border border-dashed border-black
                border-t-transparent rounded-full animate-spin' />
        }
        spinnerPlacement="end"
      >
        Loading button with hidden long text
      </Button>
    </HStack>
    ```
  </TabItem>
</Tabs>

## Polimorfismo

<Tabs>
  <TabItem label="Preview">
    <CodePreview>
      <Button as="a" href="#">Button</Button>
    </CodePreview>
  </TabItem>
  <TabItem label="Code">
    ```astro ins=/as="div"/
    ---
    import {Button} from '@kori/ui';
    ---

    <Button as="a" href="#">
      Button
    </Button>
    ```
  </TabItem>
</Tabs>

## Props

| Prop               | Type                                                                | Default     | Description                                                                  |
|--------------------|---------------------------------------------------------------------|-------------|------------------------------------------------------------------------------|
| `as`               | `astroHTML.JSX.Element`                                             | `"button"`  | Etiqueta polimórfica usada para renderizar el componente.                    |
| `shape`            | `"round" \| "sharp" \| "circle"`                                    | `"round"`   | Estilo del radio del borde.                                                  |
| `fill`             | `"solid" \| "tonal" \| "surface" \| "outline" \| "clear" \| "text"` | `"solid"`   | Estilo visual de relleno.                                                    |
| `size`             | `"xs" \| "sm" \| "md" \| "lg" \| "xl"`                              | `"md"`      | Escala de tamaño del botón.                                                  |
| `block`            | `boolean`                                                           | `false`     | Hace que el botón ocupe todo el ancho disponible.                            |
| `palette`          | `Palette` (all tailwind colors)                                     | `undefined` | Clave de color de la paleta (por ejemplo: `blue`, `emerald`, `rose`).        |
| `disabled`         | `boolean`                                                           | `false`     | Desactiva el botón.                                                          |
| `loading`          | `boolean`                                                           | `false`     | Activa el estado de carga y deshabilita la interacción mientras está activo. |
| `loadingText`      | `astroHTML.JSX.Element`                                             | `undefined` | Contenido personalizado que se muestra durante la carga.                     |
| `loadingSpinner`   | `astroHTML.JSX.Element`                                             | `undefined` | Nodo de spinner personalizado usado durante el estado de carga.              |
| `spinnerPlacement` | `"start" \| "end"`                                                  | `"start"`   | Posición del spinner cuando `loadingText` está presente.                     |
